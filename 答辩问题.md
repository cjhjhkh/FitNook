# 答辩问题准备

## 1. 项目背景与需求分析
Q：你为什么会选择“面向场景化穿搭的电子衣橱系统”作为你的毕业设计题目？
A：随着人们生活品质的提升，衣物数量增多带来了一些痛点：“记不住”导致重复购买和闲置资源浪费；“找不到”引发整理效率低下；“搭不好”造成出行、特殊场景下的决策焦虑。现有的一些电子衣橱应用往往因为场景标签过于死板（如不能结合季节与个人的实际活动），无法满足多元化、细分化的场景需求。本项目旨在通过将衣橱进行数字可视化管理，并结合场景化穿搭和AI辅助搭配，来降低用户的决策成本并提高衣物利用率。

Q：现有市场上的类似应用有哪些？你的系统和它们相比有什么创新点？
A：市面上有“搭搭”、“极简穿搭”、“LookLog”等APP，它们多聚焦于基础记录或固定场景。本系统的主要创新点在于：
1. **更加灵活的定制场景管理**：支持结合四季维度和自定义场景标签（如徒步、面试等）。
2. **日程与旅行联动（行李箱功能）**：用户能预先为出差或旅行创建“行李箱”，统一安排多天出行的物品和穿搭，生成清单以防遗漏。
3. **AI洞察与探索（ChatAI咨询等）**：引入大模型，基于用户真实的体型、肤色画像和衣橱单品给出推荐和穿搭诊断。

## 2. 系统架构与技术选型
Q：你的系统整体架构是怎样的？
A：系统采用了典型的前后端分离架构：
- **前端（客户端）**：采用 `UniApp` + `Vue3` + `TypeScript` 配合 `VantWeapp`，实现微信小程序端的轻量化操作与多端跨平台兼容。
- **后端（服务端）**：采用 `Node.js` + `Express` 提供高效的接口服务，使用 `MySQL` 存储各种核心业务结构化数据，使用 `MinIO` （对象存储）专门用于海量图片（大文件）的存储，以保证并发响应速度。

Q：为什么选择 Node.js 和 MinIO 处理图片数据？
A：Node.js 基于事件驱动、非阻塞I/O模型，非常擅长处理高并发的I/O密集型任务。本系统包含大量衣物图片资源的上传，配合 MinIO 这个高性能且拥有良好伸缩性的分布式对象存储系统，可以实现系统的图片资源与结构化数据的分离，降低数据库压力，大幅提高系统的读写与检索效率。

## 3. 核心功能实现细节
Q：请详细描述一下系统的“场景化”是如何在技术和交互上实现的？
A：在交互上，衣橱和穿搭都引入了多维标签（季节、种类、自定义场景）。在录入或生成搭配时，用户可以为各套穿搭指定场景。此外，提供日历和行李箱功能，将特定日期的穿搭安排与特定的生活场景或行程相挂钩。技术上，数据库设计了灵活的关联表和JSON字段记录复杂的配置；查询时，利用多维筛选接口从 MySQL 提取匹配的用户穿搭组合。

Q：AI 功能（如 ChatAI 咨询和根据衣橱推荐）在系统中是如何落地的？遇到了哪些难点，你是怎么解决的？
A：AI部分主要提供文字咨询、生成搭配建议和渲染模拟图。
- 难点在于让AI的回答不空泛，而是基于用户的真实衣橱数据。
- 解决思路：在发起对话请求时，系统会构建“状态快照层”，即把用户当前的身高、体重、肤色、部分衣橱数据（或刚刚点击的衣服标签）提炼成精简的上下文 Prompt 后发送给 AI（比如带入 `user_profile_snapshot` 和 `wardrobe_summary`），这样大模型的输出就具备了针对性。

Q：在你的数据库设计中，关于用户的穿搭搭配（画布）是如何保存的？
A：为了能在不同设备间无缝复现穿搭画布状态，系统保存了画布中的核心数据，包括背景颜色、尺寸、以及搭配的单品 id，并且对于每一个单品，保存了它当时使用的图片 URL 分割、在画布上的 X/Y 坐标（百分比）、旋转角度（z-index）和层级等信息，最终序列化为JSON或者存储在相应的关系表中。这种通过保存组件状态“元数据”的方法保证了即便稍后图片处理，旧搭配图依然展示稳定。

## 4. 其它技术与项目相关问题
Q：系统的性能或者安全方面做了哪些考虑？
A：
1. 图片等大体积多媒体资源用分布式对象存储 MinIO，避免了占用后端服务器大量带宽。
2. 小程序前端展示图片采用缩略图机制，优化列表加载体验。
3. 数据库表层面上对重要的一对多数据保证一致性，通过使用 Token机制维持登录安全和API权限。

Q：毕业设计中哪一部分你觉得难度最大？对未来有什么展望想进一步优化的？
A：
- **最大难点**：把多维场景的数据联动逻辑处理清楚，即单品 -> 穿搭方案 -> 行李箱打包 -> 实际穿着日记，保证这些状态能够实时同步更新，不出现脏数据，并且能在前端画布上实现缩放、旋转自如的合成交互能力。
- **未来展望**：
1. 深入接入AI图像生成或自动抠图功能，简化用户的录入工作（目前是照片录入或简单状态）；
2. 完善社区互动模块，基于用户穿搭参数（如同体重身高同体型）做智能匹配推送，增加用户的社交灵感体验。